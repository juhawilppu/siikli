{
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:packages": "npm --workspace packages run test"
  },
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ]
}