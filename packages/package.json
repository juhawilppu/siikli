{
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ]
}